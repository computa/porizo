# Web Funnel Launch QA Todo — 2026-07-15

This ledger tracks `QA-FINDINGS-2026-07-15.md` against current HEAD. The review was captured at older commit `2d437de2`; do not repeat later fixes merely because the original report still describes them.

Status meanings: **OPEN** requires production code; **IMPLEMENTED — PROOF MISSING** keeps current production code and adds regression proof; **VERIFIED BEHAVIOR — TEST MISSING** needs proof before any code change; **PARTIAL** completes only the named remainder; **EXTERNAL** belongs to another track.

## P0

- [x] #1 **FIXED** — The explicit route matrix now covers success deep links, cancel-to-offer, seven-day draft pickup, resumable theater, expired/terminal fresh entry, and malformed-state restart. The candidate persists until Resume/Start over is chosen.
- [x] #2 **FIXED** — Turnstile now has typed configuration/network/verification failures, reviewed copy, cleanup/retry and timeout proof, tracked dev/preview test configuration, and production/Docker build guards. Production site key provisioning remains external.

## P1

- [x] #3 **FIXED** — Theater stages use 0–100 thresholds with boundary-table coverage.
- [x] #4 **VERIFIED, NOT REIMPLEMENTED** — Cold `?occasion=` preselects without skipping S2; sequential regression and browser proof confirm the selected chip and date field remain visible.
- [x] #5 **VERIFIED, NOT REIMPLEMENTED** — Existing 250ms relationship auto-advance and reduced-motion fallback have timer/accessibility coverage.
- [x] #6 **FIXED** — Reset moved below the flow and destructive reset requires confirmation.
- [x] #7 **FIXED** — Entry stays at scroll zero beneath the site nav and no longer creates the large footer void at either target viewport.
- [x] #8 **VERIFIED, NOT REIMPLEMENTED** — Existing 900ms dim choreography and reduced-motion instant branch have computed browser proof.
- [x] #9 **FIXED** — The homepage Father's Day pill visibly remains Father's Day but enters the valid Custom prefill until the shared enum is extended.

## P2

- [x] #10 **FIXED** — Recipient placeholder is “Their name.”
- [x] #11 **FIXED** — Only the recipient is coral-highlighted in the lyric heading.
- [x] #12 **ALREADY FIXED — DO NOT REPEAT** — Repeated kickers were removed in `3215dee2`; refreshed `s6-preview.html` also has no dim kicker.
- [x] #13 **FIXED** — Preview shows elapsed/total timecodes and resets reachable playback state when the source changes.
- [x] #14 **FIXED** — “Make another song” sits below sharing and is also available from failed/refunded delivery states.
- [x] #15 **FIXED** — Pulse iteration count is valid and the second-listen glow runs once.
- [x] #16 **FIXED** — Both self-hosted variable fonts are preloaded.
- [x] #17 **FIXED** — Dependencies use installed compatible ranges and build tooling is in development dependencies without an upgrade.

## Coverage and launch proof

- [x] Exact draft POST bodies for `/versions` and `/lyrics/generate`.
- [x] Theater component covers the 150-second hold affordance and silent-retry-to-visible-retry flow. Production hides the hold form until the backend supplies a real callback; enabling that endpoint remains external.
- [x] Preview manual-scroll suppression and second-listen pulse.
- [x] Checkout five-second timeout, cancel return, idempotency header, and successful redirect.
- [x] Reset/Make another replaces the URL, stops polling, clears only funnel state, and preserves access/refresh tokens.
- [x] Turnstile production missing→fail, production test key→fail, production real-format fixture→pass, preview test key→pass, and Docker ARG source propagation.
- [x] 390×844 and 1440×900 integrated Chromium checks cover nav collision, footer rhythm, dim, reduced motion, horizontal rails, console, and stubbed network behavior. Media timing is covered at component level.
- [ ] Keyboard, contrast, VoiceOver S6/R1, and launch matrix: TikTok iOS, Instagram iOS, Safari iOS, Chrome Android, desktop Chrome/Safari. Mark unavailable rows as launch-blocking external evidence.
- [x] Label request-intercepted frontend journey evidence as stubbed; do not represent it as live backend integration.
- [x] Frontend lint/tests/build/bundle gate, root lint/full test, consolidated Compound review, and strict preflight. Scoped commit is the final handoff action.

## External / deliberately not implemented here

- [ ] **EXTERNAL** — Provision the real production Turnstile site key in the deployment build environment.
- [ ] **EXTERNAL** — Deliver backend U1–U3 `/web/*` endpoints or an official backend-owned mock surface.
- [ ] **EXTERNAL** — Add a backend hold-email endpoint/callback before enabling the Theater hold form in production.
- [ ] **EXTERNAL** — Extend the app/server Occasion enum with Father's Day; until then the visible homepage pill enters Custom.
