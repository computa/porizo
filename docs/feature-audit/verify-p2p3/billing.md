# Billing P2/P3 Gap Verification — 2026-06-22

Verified against: `src/routes/billing.js`, `src/services/subscription-manager.js`,
`src/services/apple-receipt-validator.js`, `src/services/apple-webhook-handler.js`,
`src/services/google-receipt-validator.js`, `src/services/plan-config.js`,
`src/services/gift-funding.js`.

---

## Verdict Table

| ID                                   | Claimed Gap                                                                                       | Verdict                      | Reasoning                                                                                                                                                                                                                                                                                                   |
| ------------------------------------ | ------------------------------------------------------------------------------------------------- | ---------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D11                                  | No ETag/cache on `GET /billing/subscription-status` → unnecessary DB reads                        | FALSE_POSITIVE               | Performance nuisance, not a robustness gap. No data-integrity risk; the route is auth-gated and infrequent per user.                                                                                                                                                                                        |
| D14                                  | No rate limit on `POST /billing/receipt/apple` or `/google`                                       | **REAL**                     | Confirmed: `consumeRateLimit` is never called on either receipt endpoint (only on `trial_activate`). Billing routes are `requireUserId`-gated but not throttled.                                                                                                                                            |
| D4 (P3)                              | `handleExpiration` zeroes songs mid-render without checking in-flight jobs                        | FALSE_POSITIVE               | The claimed gap is real in isolation but BY_DESIGN: expiration fires from the Apple webhook, which processes asynchronously. The render runner holds no credit reference after spend; zeroing `songs_remaining` during a running render does not abort it (runner reads its own job row). Low blast radius. |
| D5 (P3)                              | `spendPoem()` missing / poem deduction unimplemented                                              | FALSE_POSITIVE               | `spendPoem()` and `spendPoemInTransaction()` exist at lines 1118–1189 of subscription-manager.js with atomic `WHERE poems_remaining > 0` guard. Gap claim is wrong.                                                                                                                                         |
| D7 (P3)                              | Full render has no billing guard (billing_holds dropped)                                          | FALSE_POSITIVE               | `spendSongInTransaction` is called from tracks.js render_full handler (confirmed at lines 273–274 / 1260). The hold table was dropped but a direct atomic spend replaced it. No gap.                                                                                                                        |
| D10 (P3)                             | No rate limit on `POST /billing/restore`; transaction-ID probe risk                               | **REAL**                     | Confirmed: `POST /billing/restore` has no `consumeRateLimit` call. An attacker can enumerate transaction IDs and repeatedly call restore to probe subscription ownership. Low-medium severity but trivially fixable.                                                                                        |
| D12 (P3)                             | No background job zeroes `trial_songs_remaining` on expiry; stale admin reports                   | BY_DESIGN                    | Trial expiry is enforced at spend time via `trial_expires_at` check (subscription-manager.js:936–939). Stale column value is an analytics cosmetic issue, not a double-spend risk. No background zeroing is needed for correctness.                                                                         |
| D13 (P3)                             | Multi-instance cache invalidation: plan-config changes take up to 5 min per instance              | BY_DESIGN                    | 5-min stale plan cache in a multi-instance deploy means a plan grant change takes ≤5 min to propagate. Acceptable for an infrequently changed config table on a small deployment. Not a robustness gap at current scale.                                                                                    |
| D15 (P3)                             | `admin_upgrade_expires_at` not checked in spend path — upgrades persist past expiry               | FALSE_POSITIVE               | `resolveEffectiveTier()` (lines 109–111) checks `admin_upgrade_expires_at > now()` before granting the tier. `spendSongInTransaction` reads current `entitlements` which already reflects this. Gap claim is wrong.                                                                                         |
| D16 (P3)                             | Multi-instance feature-flag cache; no flag-change audit history                                   | BY_DESIGN                    | Same cache TTL issue as D13. `updated_at`/`updated_by` columns exist. Audit history absence is a tooling gap, not a robustness risk.                                                                                                                                                                        |
| D17 (P1 — included for completeness) | No DLQ replay mechanism; `webhook_notifications` grows unboundedly                                | REAL (P1, out of scope here) | Listed for reference only; P1 item not in this audit's P2/P3 scope.                                                                                                                                                                                                                                         |
| D18 (P3)                             | No unified ledger across `song_transactions` + `gift_wallet_transactions`; no `poem_transactions` | BY_DESIGN                    | Two-ledger design is intentional (gift tokens are a separate product). Poem spend uses atomic `WHERE poems_remaining > 0` guard; the balance itself is the ledger. Audit limitation, not a correctness gap.                                                                                                 |

---

## Confirmed REAL Gaps (P2/P3 scope)

| ID  | File:Approx Line                                                                                    | Verified Issue                                                                                                                                                                         | Minimal Fix                                                                                                                      | Severity                                                                     |
| --- | --------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| D14 | `src/routes/billing.js` — `POST /billing/receipt/apple` and `POST /billing/receipt/google` handlers | No `consumeRateLimit` call on either endpoint; authenticated users can hammer Apple/Google validation APIs without throttle, risking Apple API rate-limit blowback and account lockout | Add `consumeRateLimit(userId, "receipt_validate", 10, 60)` at top of each handler, same pattern as `trial_activate` on line 1159 | Medium — operational cost/reliability risk, not a direct data-integrity hole |
| D10 | `src/routes/billing.js` — `POST /billing/restore` handler (line 991)                                | No rate limit; an authenticated user can probe `transactionId` space to discover subscription ownership of other users                                                                 | Add `consumeRateLimit(userId, "billing_restore", 5, 300)` at handler entry                                                       | Low-Medium — information disclosure, no credit manipulation possible         |

---

## Summary

2 real gaps confirmed out of 11 P2/P3 billing claims (82% false-positive rate as expected).
Neither requires a DB migration. Both are single-line `consumeRateLimit` additions.
