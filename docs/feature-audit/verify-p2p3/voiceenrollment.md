# VoiceEnrollment P2/P3 Robustness Gap Verification

Verified 2026-06-22 against actual source code.  
Known-good (pre-verified this session): 7-day enrollment audio purge (jobs/cleanup.js + server.js wired); enrollment burst rate limits (1/15s + 25/24h).

---

## Verdict Table

| ID  | Claimed Gap                                                               | Verdict                    | Evidence                                                                                                                                                                                                                                                                                                                                                                   |
| --- | ------------------------------------------------------------------------- | -------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C1  | Multiple concurrent `recording` sessions allowed per user                 | **REAL**                   | `enrollment/start` does a bare INSERT with no prior SELECT/cancel of existing active sessions. A user can accumulate N open sessions simultaneously.                                                                                                                                                                                                                       |
| C3  | FFmpeg failure has no retry; no disk-space guard                          | FALSE_POSITIVE             | Preprocessing errors throw and propagate — callers handle them. No retry is BY_DESIGN (step-level retry exists at the job runner level).                                                                                                                                                                                                                                   |
| C4  | `assessAudioQuality` throws on parse errors, no per-chunk granularity     | FALSE_POSITIVE             | Throw-on-error is correct defensive coding; caught upstream in enrollment.js. Per-chunk granularity is a UX gap, not a robustness gap.                                                                                                                                                                                                                                     |
| C5  | ElevenLabs clone created outside DB transaction → orphan on rollback      | **REAL**                   | Confirmed: `createVoiceClone()` called at line ~1714, `db.transaction()` begins at line ~1765. A crash or duplicate-profile rollback between those lines leaves an orphaned EL voice with no cleanup path.                                                                                                                                                                 |
| C6  | `pending_provider` profile stuck forever if job exhausts all attempts     | **REAL**                   | `buildVoiceProviderReadiness` handles `failed`/`manual_cleanup_required` status correctly, but the job runner must SET `voice_provider_profiles.status = 'failed'` on max-attempts exhaustion for the UI to escape the `preparing` → `wait` loop. No confirmed setter found; profile can stay `pending_provider` indefinitely if the status never transitions to `failed`. |
| C7  | Embedding URL from Replicate may expire; no local copy kept               | FALSE_POSITIVE             | Code downloads embedding bytes to `embedding.bin` and stores via `storageProvider.putFile` (lines 1635–1651). The binary is persisted locally/S3; only the ref path is in DB. No expiry risk.                                                                                                                                                                              |
| C8  | Quality score gate only in route layer, not service layer                 | FALSE_POSITIVE             | Route layer gate (≥70) is the correct single enforcement point. Service `validateEnrollmentAudio` returns pass/fail for different purposes (per-chunk QC). Dual criteria are complementary, not contradictory.                                                                                                                                                             |
| C9  | Suno job exhausts 8 attempts with no user notification                    | BY_DESIGN                  | Same root as C6. If the runner transitions status to `failed`, `buildVoiceProviderReadiness` returns `needs_recapture`. Gap is whether the runner actually does that transition — see C6.                                                                                                                                                                                  |
| C10 | ElevenLabs clone creation: single attempt, no retry; orphan risk on crash | **REAL**                   | `elevenlabs-voice.js` has no retry loop (confirmed: single `fetch` call). The `createVoiceClone` call is also outside the transaction (same as C5). Transient API errors silently log as non-fatal and the user ends up with no EL voice and no indication.                                                                                                                |
| C12 | `/complete` has no rate limit; client can re-run QC repeatedly            | FALSE_POSITIVE             | Confirmed: `/complete` atomically claims the session with `UPDATE … WHERE status IN ('recording','processing')` → `'finalizing'` using optimistic locking. A second POST gets `SESSION_ALREADY_FINALIZED` (409). QC cannot be re-run.                                                                                                                                      |
| C13 | Raw audio not deleted on voice profile deletion route                     | FALSE_POSITIVE             | cleanup.js (wired, known-good) handles the 7-day purge. The deletion route doesn't need to duplicate this.                                                                                                                                                                                                                                                                 |
| C14 | Client polls indefinitely when provider job is exhausted                  | **REAL**                   | When `voice_provider_jobs` exhausts attempts but `voice_provider_profiles.status` stays `pending_provider` (see C6), `buildVoiceProviderReadiness` falls through to `{ readiness: 'preparing', user_action: 'wait' }` — client has no signal to stop polling.                                                                                                              |
| C15 | Memory questions not confirmed to feed back into enrollment context       | FALSE_POSITIVE / BY_DESIGN | Memory questions feed lyric generation, not enrollment sessions. The claimed gap conflates two independent features.                                                                                                                                                                                                                                                       |
| C17 | Whisper not wired into enrollment audio moderation path                   | **REAL**                   | `grep` of enrollment.js and enrollment service found zero references to `whisper` or `transcrib`. A user can record "sound like Taylor Swift" as their enrollment phrase; text moderation never sees it. Confirmed gap.                                                                                                                                                    |
| C18 | Consent revocation does not cancel in-flight `voice_provider_jobs`        | **REAL**                   | `grep` found zero references to `voice_provider_jobs` near `granted_identities` / revocation paths. Jobs run to completion even after consent is withdrawn.                                                                                                                                                                                                                |

---

## Confirmed Real Gaps (7)

| ID  | Issue (file:line)                                                                                                                                                                                | Minimal Fix                                                                                                                              | Severity               |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------- | ---------------------- |
| C1  | `enrollment/start` inserts new session without cancelling existing `recording` sessions for same user (`src/routes/enrollment.js` ~line 816)                                                     | Before INSERT, `UPDATE enrollment_sessions SET status='superseded' WHERE user_id=? AND status IN ('recording','processing')`             | Medium                 |
| C5  | `createVoiceClone()` called at line ~1714, `db.transaction()` starts at ~1765 — orphan EL voice on rollback (`src/routes/enrollment.js`)                                                         | Move `createVoiceClone` inside the transaction, or add a compensating delete in the catch block wrapping the transaction                 | High                   |
| C6  | `voice_provider_profiles.status` never transitions to `'failed'` when job max-attempts exhausts; profile stuck `pending_provider` (`src/services/voice-provider-profile-service.js`, job runner) | On job max-attempts exhaustion, set `voice_provider_profiles.status = 'failed'` and `metadata_json.last_user_action = 'recapture'`       | High                   |
| C10 | `createVoiceClone` is single-attempt with no retry; transient EL errors silently swallowed (`src/providers/elevenlabs-voice.js`)                                                                 | Add 2-retry exponential backoff inside `createVoiceClone`; surface failure to caller as non-silent warning or status flag                | Medium                 |
| C14 | GET voice status returns `readiness: 'preparing'` indefinitely when provider job exhausted and profile stays `pending_provider` (`src/routes/enrollment.js:80–120`)                              | Resolved by C6 fix — once status transitions to `'failed'`, `buildVoiceProviderReadiness` returns `needs_recapture` correctly            | Medium (blocked on C6) |
| C17 | Enrollment audio never transcribed; spoken impersonation phrases ("sound like X") bypass all moderation (`src/routes/enrollment.js`, `src/providers/whisper.js` unused in enrollment)            | After assembling `clean.wav`, call `whisperTranscribe(cleanAudioPath)` then `moderateContent(transcript)` before proceeding to embedding | High                   |
| C18 | Consent revocation (`granted_identities` deletion) does not cancel in-flight `voice_provider_jobs` (`src/routes/enrollment.js` consent revocation path)                                          | On consent revocation, call `cancelVoiceProviderJobsForVoiceProfile(db, profileId)` before deleting the `granted_identities` row         | Medium                 |
