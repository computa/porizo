# Root 1 Auth Username Availability Repository - Adversarial Review 1

Date: 2026-06-28

## Scope

- Extended `auth-profile-repository.js` with `findActiveUserByUsername()`.
- Replaced route-local username availability SQL in `routes/auth.js` for:
  - requested username existence check,
  - generated suggestion collision checks.

## Boundary

This slice intentionally does not move username format validation, IP rate
limiting, suggestion generation/randomness, error messages, or response shape.
Those remain in the route because they are HTTP/product policy rather than
profile persistence.

## Adversarial Findings

- **P0:** None.
- **P1:** None.
- **P2 VERIFIED:** The repository preserves the previous soft-delete exclusion:
  deleted users do not block username availability.
- **P2 VERIFIED:** Suggestions are still generated by the route and are checked
  one at a time against the same active-username predicate.

## Risks Checked

- **Enumeration boundary:** Rate limiting stays in `routes/auth.js`.
- **Product boundary:** Username format copy and availability envelope are
  unchanged.
- **Agent resource management:** no new subagents were launched because inherited
  agent sessions timed out and `close_agent` previously stalled; local bounded
  parallel commands were used instead.

## Validation

- `node --check src/database/auth-profile-repository.js`
- `node --check src/routes/auth.js`
- `NODE_ENV=test ALLOW_ANON_USER_ID=true ALLOW_DEVICE_TOKEN_FALLBACK=true node --test --test-concurrency=1 test/auth-profile-repository.test.js`
- `NODE_ENV=test ALLOW_ANON_USER_ID=true ALLOW_DEVICE_TOKEN_FALLBACK=true node --test --test-concurrency=1 test/auth-api.test.js`
- `npm run lint`
- `git diff --check -- src/routes/auth.js src/database/auth-profile-repository.js test/auth-profile-repository.test.js`
