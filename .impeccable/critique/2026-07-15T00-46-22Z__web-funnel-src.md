---
timestamp: 2026-07-15T00-46-22Z
slug: web-funnel-src
---
# Web funnel critique — 2026-07-15

Target: `web-funnel/src`

## Verdict

Warm Canvas is centered, emotionally coherent, responsive without horizontal overflow, and does not broadly read as AI-generated. Fraunces and the cream palette are intentional identity choices. Release is blocked by interaction, accessibility, and signature-motion defects.

## Findings

1. P1 — Mobile S4 voice controls remain behind the fixed CTA even at maximum scroll. Restore tokenized bottom clearance including safe-area inset.
2. P1 — Editing a completed answer replaces the stack instead of expanding the row in place, losing downstream context and focus. Separate edit state from live-step progress.
3. P1 — White on coral CTA text is 3.01:1; focus ring and inactive preview lyrics also miss contrast requirements. Use verified token pairs.
4. P1 — The signature dim is an immediate background swap followed by content opacity, not the specified 900ms luminance/lamplight transition.
5. P1 — Preview fixture audio is missing and playback rejection/error has no recoverable UI.
6. P2 — Offer CTA does not follow the fixed-mobile CTA contract.
7. P2 — Preview scrub lacks progressbar semantics; theater updates are not live-announced; memory error linkage is incomplete.
8. P2 — Mobile Sign in hit target is below 44px.
9. P2 — Reduced-motion generation bar uses 100%, contrary to the specified static 50% state.
10. P2 — Source fidelity drift: repeated site-wide tracked-uppercase eyebrow treatment, missing memory coaching line, and abrupt native placeholder rotation.

## Intentional choices

- Fraunces display face.
- Warm Canvas cream background and warm radial desktop atmosphere.
- Approved 20px card radius.
- Stacked-collapse interaction model.
- Dim playback scene.

## Baseline validation

- `npm run lint`: passed.
- `npm test`: 40/40 passed.
- `npm run build`: passed; font URLs remain runtime-resolved under `/create/fonts/`.
